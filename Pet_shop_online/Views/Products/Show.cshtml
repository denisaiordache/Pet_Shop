
@{
    ViewBag.Title = "Show";
}

<h2>Afisare Produs</h2>
<br />
<a href="/Products/Index">Inapoi la produse</a>
<br />

<div class="panel panel-default">
    <div class="panel-heading">@ViewBag.Product.ProductName</div>
    <div class="panel-body">
        Descriere produs: <strong>@ViewBag.Product.Description</strong>
        <br />
        <strong>@ViewBag.Product.Price Lei</strong>
        <br />

        <i class="glyphicon glyphicon-list"></i> @ViewBag.Category.CategoryName
        <i class="glyphicon glyphicon-heart-empty"></i> @ViewBag.Animal.Species
        <br />
        <span class="label label-info">@ViewBag.Product.Stock Produse disponibile</span>
        <span class="label label-success">@ViewBag.Product.Date</span>
        <hr>
        <a class="btn btn-success pull-left" href="/Products/Edit/@ViewBag.Product.ProductID">Modifica produs</a>
        <form method="post" action="/Products/Delete/@ViewBag.Product.ProductID">
            @Html.HttpMethodOverride(HttpVerbs.Delete)
            <button class="btn btn-danger pull-right" type="submit">Elimina produs</button>
        </form>
    </div>
</div>
<br />
<br />
<hr />
@if (ViewBag.Product.Comments.Count == 1)
{
    <h3>1 comentariu</h3>
}
else if (ViewBag.Product.Comments.Count == 0)
{
    <h3>Niciun comentariu</h3>
}
else if (ViewBag.Product.Comments.Count < 20)
{
    <h3>@ViewBag.Product.Comments.Count comentarii</h3>
}
else
{
    <h3>@ViewBag.Product.Comments.Count de comentarii</h3>
}

<br /><br />
@foreach (var comm in ViewBag.Product.Comments)
{
    <div class="container">
        <div class="row">
            <div class="col-md-2"></div>
            <div class="col-md-8">
                <div>
                    <p>@comm.Content</p>
                    <small class="pull-right">@comm.Date</small>
                </div>
                <div style="display: inline-block; float : left; margin-right: 15px">
                    <a class="glyphicon glyphicon-pencil" href="/Comments/Edit/@comm.CommentID"></a>
                </div>
                <div style="display: inline-block; float : left">
                    <form method="post" action="/Comments/Delete/@comm.CommentID">
                        @Html.HttpMethodOverride(HttpVerbs.Delete)
                        <button class="glyphicon glyphicon-trash" style="border: none; background: transparent" type="submit"></button>
                    </form>
                </div>
            </div>
        </div>
        <div class="col-md-2"></div>
    </div>
    <hr />
}

<div class="container">
    <div class="row">
        <div class="col-md-2">
        </div>
        <div class="col-md-8">
            <div class="panel">
                <form method="post" action="/Comments/New">
                    <div class="panel-body">
                        <input type="hidden" name="ProductID" value="@ViewBag.Product.ProductID" />
                        <label>Continut comentariu</label>
                        <br />
                        <textarea class="form-control" name="Content"></textarea>
                        <br />
                        <button class="btn btn-success " type="submit">Adauga comentariu</button>
                    </div>
                </form>
            </div>
        </div>
        <div class="col-md-2"></div>
    </div>
</div>
